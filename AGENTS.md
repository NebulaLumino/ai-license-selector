# AGENTS.md

Standard Next.js AI app workspace.

## Commands

```bash
npm install
npm run dev    # development
npm run build  # production build
npm start      # start production server
```

## Environment

- `OPENAI_API_KEY` — DeepSeek API key (or any OpenAI-compatible key)
